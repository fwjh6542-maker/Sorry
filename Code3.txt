<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Will You Be My Madam Jii?</title>
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
<script src="https://cdn.tailwindcss.com"></script>
<style>
body {
background: white;
}

.heart, .rose {
position: fixed;
font-size: 24px;
pointer-events: none;
animation: fall linear forwards;
}

@keyframes fall {
to {
transform: translateY(100vh);
}
}

.sticker {
animation: none;
width: 200px;
height: auto;
display: block;
margin: 0 auto 1.5rem;
}

h1 {
background: linear-gradient(135deg, #dc143c 0%, #8b00ff 100%);
-webkit-background-clip: text;
-webkit-text-fill-color: transparent;
background-clip: text;
font-size: 3rem;
font-weight: bold;
margin-bottom: 1.5rem;
}

@media (max-width: 768px) {
.sticker {
width: 150px;
}
}

@media (max-width: 480px) {
.sticker {
width: 120px;
}
}

.btn-hover {
background: linear-gradient(135deg, #dc143c 0%, #8b00ff 100%);
color: white;
font-weight: bold;
transition: transform 0.3s ease;
}

.btn-hover:hover {
transform: scale(1.1);
}
</style>
</head>
<body class="min-h-screen bg-gradient-to-br from-pink-50 via-rose-100 to-purple-100 flex flex-col items-center justify-center overflow-hidden p-4">
<!-- Main Content -->
<div class="text-center z-10 w-full max-w-2xl">
<!-- Proposal Text -->
<h1 class="text-3xl md:text-4xl lg:text-5xl font-bold gradient-text mb-6 md:mb-8 animate-pulse">
Can you be my Madam Jii??
</h1>

<!-- Sticker -->
<img src="./pic3.png" alt="Sticker" class="sticker mb-6 md:mb-10">

<!-- Buttons Container -->
<div class="flex justify-center items-center gap-4 md:gap-6">
<button id="yesBtn"
class="btn-hover bg-gradient-to-r from-pink-400 to-rose-400 hover:from-pink-500 hover:to-rose-500 text-white font-bold py-3 px-8 md:py-4 md:px-10 rounded-full shadow-lg transition-all text-lg md:text-xl">
Yes
</button>
<button id="noBtn"
class="btn-hover bg-gradient-to-r from-purple-400 to-pink-400 hover:from-purple-500 hover:to-pink-500 text-white font-bold py-3 px-8 md:py-4 md:px-10 rounded-full shadow-lg transition-all text-lg md:text-xl">
No
</button>
</div>
</div>

<script>
// Function to create falling elements
function createFallingElement(type) {
const element = document.createElement('div');
element.className = type === 'heart' ? 'heart' : 'rose';
element.innerHTML = type === 'heart' ? '❤️' : '🌹';
element.style.left = Math.random() * 100 + 'vw';
element.style.animationDuration = (Math.random() * 3 + 2) + 's';
document.body.appendChild(element);

// Remove element after animation
element.addEventListener('animationend', () => element.remove());
}

// Create falling hearts and roses periodically
setInterval(() => createFallingElement('heart'), 100);
setInterval(() => createFallingElement('rose'), 200);

// Button handling
const yesBtn = document.getElementById('yesBtn');
const noBtn = document.getElementById('noBtn');
let scale = 1;

noBtn.addEventListener('click', () => {
scale += 0.5;

yesBtn.style.transform = `scale(${scale})`;

// Shake animation for No button
gsap.to(noBtn, {
x: "+=10",
duration: 0.1,
yoyo: true,
repeat: 5
});
});

yesBtn.addEventListener('click', () => {
// Celebration animation
gsap.to('.sticker', {
scale: 2,
duration: 0.5,
yoyo: true,
repeat: 1
});

// Create burst of hearts
for(let i = 0; i < 20; i++) {
setTimeout(() => createFallingElement('heart'), i * 50);
}

// Smooth transition effect
gsap.to('body', {
opacity: 0,
duration: 1,
onComplete: () => {
window.location.href = 'third.html';
}
});
});
</script>
</body>
</html>